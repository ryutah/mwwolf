{
	"private": true,
	"scripts": {
		"lint:prettier": "prettier --check",
		"fix:prettier": "npm run lint:prettier -- --write",
		"postinstall": "husky install"
	},
	"devDependencies": {
		"husky": "^5.1.0",
		"lint-staged": "^10.5.4",
		"prettier": "^2.2.1"
	}
}
